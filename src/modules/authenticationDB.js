import { logins } from "./logins.js";

//RETURNS TRUE IF EMAIL & PASSWORD MATCH A LOGIN
//RETURNS FALSE IF EMAIL & PASSWORD DON'T MATCH A LOGIN
export function authenticationDB(email = "", password = "") {
	const apiKey = "ryj2213"
	const client = new DatabaseSimulation(apiKey);

}

